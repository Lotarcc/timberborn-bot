# Secrets & local config — never commit
.env
.env.*
*.local
secrets*
*.key
*.pem
*.pfx

# .NET / Unity mod build artifacts
bin/
obj/
*.user
*.suo
*.csproj.user
[Bb]uild/
[Tt]emp/
[Ll]ibrary/
*.dll
*.pdb

# Python (agent)
__pycache__/
*.pyc
.venv/
venv/
.mypy_cache/
.pytest_cache/

# Node (dashboard/tooling)
node_modules/
dist/

# Models & large binaries
*.gguf
*.onnx
*.pt
# Regenerable ML training artifact (Task 5 rebuilds it every retrain; keep history lean).
# The small portable runtime models (decision_mlp.json/cart.json/labels/vocab) stay tracked.
/agent/data/decision_dataset.json

# Timberborn saves (force-add specific test checkpoints with `git add -f`)
*.timber

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp

# agent run artifacts
agent/journal/
agent/metrics.csv

# generated embedding index (rebuild with: python3 agent/kb.py --build-index)
agent/kb_index.json
/agent/data/decision_dataset.pristine.json
